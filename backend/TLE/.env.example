# Example backend environment variables for SatTracker microservice
# Copy to `.env` or set in your environment before running the backend.

# Optional Space-Track credentials to enable direct Space-Track fetching
# (leave blank to use public CelesTrak fallback)
SPACETRACK_USERNAME=your_spacetrack_username
SPACETRACK_PASSWORD=your_spacetrack_password

# Recommended to set the listening URL for Kestrel, e.g. http://localhost:5000
ASPNETCORE_URLS=http://localhost:5000
